# weareactiveai.com

Astro rebuild of the Active AI marketing site. See `CLAUDE.md` for stack, commands, brand and content rules.

```sh
npm install
npm run dev     # http://localhost:4321
npm run build   # type check + build + content rules
```
